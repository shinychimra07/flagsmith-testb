version: "1.0"
services:
  - name: flagsmith-api
    language: python
    framework: pytest
    outputDir: api/tests
    api:
      baseUrl: http://localhost:8080/api/v1
      authHeader: Authorization
      authType: token
